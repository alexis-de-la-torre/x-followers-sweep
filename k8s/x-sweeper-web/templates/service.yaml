apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-sweeper-web.fullname" . }}
  labels:
    {{- include "x-sweeper-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "x-sweeper-web.selectorLabels" . | nindent 4 }}
