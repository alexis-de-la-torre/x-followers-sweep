apiVersion: v1
kind: Service
metadata:
  name: {{ include "sweeper-agent.fullname" . }}
  labels:
    {{- include "sweeper-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sweeper-agent.selectorLabels" . | nindent 4 }}