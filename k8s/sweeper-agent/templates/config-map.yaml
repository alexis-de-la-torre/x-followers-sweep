apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sweeper-agent.fullname" . }}-config
  labels:
    {{- include "sweeper-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}